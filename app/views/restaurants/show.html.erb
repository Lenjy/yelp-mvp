<div class="container">
  <h3><%= @restaurant.name %></h3>
  <p><%= @restaurant.category %></p>
  <p><%= @restaurant.address %></p>
  <% @restaurant.reviews.each do |review|%>
  <p><%=  review.content %> : <%= review.rating %> stars</p>
  <% end %>
  <%= link_to 'Go to homepage', restaurants_path %>
</div>